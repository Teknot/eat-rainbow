//@ts-nocheck

import React from 'react';
import {Text, View} from './Themed';

export default function RegisterMeals() {
  return (
    <View
      style={{
        height: '100%',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>How TO Register Meals</Text>
    </View>
  );
}
